resources:
- name: git-pollen-aboutme
  type: git
  source:
    uri: https://github.com/aquarhead/pollen-aboutme.git
    branch: master

jobs:
- name: test-render
  plan:
  - get: pollen-aboutme
    resource: git-pollen-aboutme
    trigger: true
  - task: render
    file: pollen-aboutme/ci/render.yml
