platform: linux

image_resource:
  type: docker-image
  source:
    repository: aquarhead/pollen

inputs:
- name: pollen-aboutme

outputs:
- name: published

run:
  path: ./pollen-aboutme/ci/publish.sh
